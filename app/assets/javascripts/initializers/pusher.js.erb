<% raise StandardError, "Cannot generate pusher.js without PUSHER_KEY" unless ENV['PUSHER_KEY'] %>
if('undefined' !== typeof(Pusher)) {
  NTC.pusher = new Pusher('<%= ENV['PUSHER_KEY'] %>');
  NTC.channel = NTC.pusher.subscribe('global');
}
